export default function AppLoading() {
  return (
    <main className="page" aria-busy="true" aria-live="polite">
      <section className="panel stack">
        <h1>Loading AI Council</h1>
        <p className="muted">Preparing your workspace.</p>
      </section>
    </main>
  );
}
